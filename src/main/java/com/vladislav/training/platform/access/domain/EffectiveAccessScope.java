package com.vladislav.training.platform.access.domain;

/**
 * Перечисление {@code EffectiveAccessScope}.
 */
public enum EffectiveAccessScope {
    NONE,
    GLOBAL,
    UNIT_ONLY,
    UNIT_SUBTREE
}
